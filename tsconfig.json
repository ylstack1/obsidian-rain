{
    "compilerOptions": {
      "baseUrl": ".",
      "inlineSourceMap": true,
      "inlineSources": true,
      "module": "ESNext", // Use ES Modules for modern tooling like esbuild
      "target": "ES6", // Target modern JavaScript environments (like Obsidian's Electron)
      "allowJs": true, // Allow JavaScript files to be compiled
      "noImplicitAny": true, // Require explicit 'any' type annotations
      "moduleResolution": "node", // Use Node.js style module resolution
      "importHelpers": true, // Use tslib helpers to reduce code duplication
      "isolatedModules": true, // Treat each file as a separate module (required by some tools)
      "strictNullChecks": true, // Enable strict null checks
      "lib": [
        "DOM",
        "ES5",
        "ES6",
        "ES7"
      ], // Include necessary library definitions
      "types": [
        "node"
      ]
    },
    "include": ["src/**/*"] // Specifies which files TypeScript should compile (all .ts files in src)
  } 